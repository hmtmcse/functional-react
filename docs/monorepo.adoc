```bash
npm install -g pnpm

mkdir my-monorepo
cd my-monorepo
pnpm init

# create pnpm-workspace.yaml


mkdir apps
cd apps
pnpm create vite web --template react-ts

cd ..
mkdir packages
cd packages
mkdir ui
cd ui
pnpm init
mkdir src

pnpm add -D vite-tsconfig-paths --filter web
```
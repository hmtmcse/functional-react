
== UI
* xxx-page.tsx : Must add page extension if it direct access from url
* xxx-dialog.tsx : If it open by dialog
* xxx-view.tsx : Not directly open but common view which will share by others


== Need to address
* state
** setState
* props
** defaultProps
* globalContext
** globalConfig
* pageTitle
* didMount
* didUpdate
* redirect
* showFlash
* form
** fieldDefinition
** inputField
** validation
** field store data should not store in state
* middleware


== UI HTML Component
* Button
* Card
** CardBody
** Header Footer - Explore if needed or not
* Row
* Column
* Accordion
* Form
* Pagination
* FlashBox
* Container
* Input
** InputFiled
** Select
** Label


== defaultProps
```ts
export default function Button({
  label,
  onClick,
  variant = "primary",
  disabled = false,
}: ButtonProps) {
  return (
    <button onClick={onClick} disabled={disabled} className={`btn-${variant}`}>
      {label}
    </button>
  )
}
```